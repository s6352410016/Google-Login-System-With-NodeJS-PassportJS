<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    <% if(fullname.length > 0 && photo.length > 0){ %>
        <h1><img src="<%= photo %>" width="50px" height="50px"> Hello <%= fullname%></h1>
        <a href="/logout">Logout</a>
    <% } %>
</body>
</html>